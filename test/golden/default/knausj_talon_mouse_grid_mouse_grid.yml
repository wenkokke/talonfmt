input: "tag: user.mouse_grid_enabled\n-\nM grid:\n    app.notify(\"please use the\
  \ voice command 'mouse grid' instead of 'm grid'\")\n    user.grid_select_screen(1)\n\
  \    user.grid_activate()\n"
output: |2-
   tag: user.mouse_grid_enabled
  -

  M grid:
      app.notify("please use the voice command 'mouse grid' instead of 'm grid'")
      user.grid_select_screen(1)
      user.grid_activate()
