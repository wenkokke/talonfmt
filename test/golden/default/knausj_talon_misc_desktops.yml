input: "desk <number_small>: user.desktop(number_small)\ndesk next: user.desktop_next()\n\
  desk last: user.desktop_last()\ndesk show: user.desktop_show()\nwindow move desk\
  \ <number>: user.window_move_desktop(number)\nwindow move desk left: user.window_move_desktop_left()\n\
  window move desk right: user.window_move_desktop_right()\n"
output: |-
  -

  desk <number_small>:
      user.desktop(number_small)
  desk next:
      user.desktop_next()
  desk last:
      user.desktop_last()
  desk show:
      user.desktop_show()
  window move desk <number>:
      user.window_move_desktop(number)
  window move desk left:
      user.window_move_desktop_left()
  window move desk right:
      user.window_move_desktop_right()
